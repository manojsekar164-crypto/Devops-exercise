apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: default
  labels:
    app.kubernetes.io/name: external-secrets-sa
    app.kubernetes.io/component: credential-management
    app.kubernetes.io/part-of: payment-system
  annotations:
    # IAM Role for Service Accounts (IRSA) configuration
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/eks-external-secrets-role
